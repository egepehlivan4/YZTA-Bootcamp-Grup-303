# FloraGuard — Streamlit Frontend Bağımlılıkları (Streamlit Community Cloud)
#
# ÖNEMLİ: Streamlit Community Cloud, giriş dosyasının (src/ui/streamlit_app.py)
# konumundan BAĞIMSIZ olarak HER ZAMAN reponun kök dizinindeki bu dosyayı okur
# (giriş dosyasıyla aynı dizindeki requirements.txt'i tercih ETMEZ). Bu yüzden
# bu dosya bilinçli olarak yalnızca arayüzün gerçekten kullandığı 3 paketi
# içerir; backend'in tam bağımlılık listesi (FastAPI, torch, langchain, vb.)
# `requirements-full.txt` içindedir ve Docker/Render tarafından kullanılır
# (bkz. Dockerfile). Yerel tam-yığın (backend+frontend) geliştirme için:
#     pip install -r requirements-full.txt
streamlit==1.40.2
requests==2.32.3
pandas==2.2.3
